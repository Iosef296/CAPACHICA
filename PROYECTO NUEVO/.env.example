# Copia este archivo a .env y ajusta los valores para tu entorno.
# docker-compose lo lee automaticamente.

# --- Base de datos PostgreSQL ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=arcangelpro4321
POSTGRES_DB=capachica_db

# --- Backend ---
# Origen permitido para CORS (el frontend)
CORS_ORIGIN=http://localhost:4321
# Secreto para firmar los tokens JWT (cambialo en produccion)
JWT_SECRET=capachica_secret_2026
