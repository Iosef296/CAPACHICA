---
import Layout from '../layouts/Layout.astro';
import Artesania from '../components/Artesania';

const IA_URL = 'http://localhost:5000';
let cms: any = null;
try { const r = await fetch(`${IA_URL}/api/contenido/artesania`); if (r.ok) cms = await r.json(); } catch {}
const cmsHero = cms?.secciones?.find((s: any) => s.tipo === 'hero');
---
<Layout title="Artesanía — Capachica">
  <Fragment slot="hero">
    <span class="section-label">ARTESANÍA · CAPACHICA</span>
    <h1 style="font-family:var(--font-display);font-size:clamp(3rem,7vw,5.5rem);line-height:1.05;color:var(--text-main);margin-bottom:0.4rem;">
      {cmsHero?.titulo || 'Artesanía'}
    </h1>
    <p style="font-size:clamp(1rem,2vw,1.1rem);color:var(--text2);max-width:480px;margin:0 auto 2rem;line-height:1.7;">
      {cmsHero?.subtitulo || 'Tejidos de alpaca, cerámica andina y artesanías hechas a mano centenarias.'}
    </p>
  </Fragment>
  <Artesania client:load hideHero={true} />
</Layout>